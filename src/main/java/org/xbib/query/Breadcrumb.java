package org.xbib.query;

/**
 * A bread crumb interface. If an object implements Breadcrumb,
 * it can be a component of a breadcrumb trail.
 */
public interface Breadcrumb extends Comparable<Breadcrumb> {
}
